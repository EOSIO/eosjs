{
  "name": "eosjs2",
  "version": "0.0.0",
  "description": "Talk to eos API",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack -o dist/eosjs2-rel.js -p --output-library eosjs2 && webpack -o dist/eosjs2-debug.js --mode development --debug --devtool inline-source-map --output-pathinfo --output-library eosjs2"
  },
  "author": "block.one",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/EOSIO/eosjs2.git"
  },
  "devDependencies": {
    "eosjs-ecc": "^4.0.1",
    "webpack": "^4.10.2",
    "webpack-cli": "^3.0.1"
  }
}
