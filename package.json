{
  "name": "eos-altjs",
  "version": "0.0.0",
  "description": "Talk to eos API",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack -o dist/eos-altjs-rel.js -p && webpack -o dist/eos-altjs-debug.js --mode development --debug --devtool inline-source-map --output-pathinfo"
  },
  "author": "Todd Fleming",
  "license": "MIT",
  "devDependencies": {
    "eosjs-ecc": "^4.0.0",
    "webpack": "^4.6.0",
    "webpack-cli": "^2.0.14"
  }
}
